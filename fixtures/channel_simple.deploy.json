{
  "version": "0.1.0",
  "channels": {
    "note-sync": {
      "url": "https://TODO-configure-note_sync.example.com/api",
      "protocol": "http",
      "auth": {
        "type": "bearer",
        "token": "${NOTE_SYNC_TOKEN}"
      },
      "timeout_ms": 30000,
      "retry": {
        "max_attempts": 3,
        "backoff_ms": 1000
      }
    },
    "echo-receiver": {
      "url": "https://TODO-configure-echo_receiver.example.com/api",
      "protocol": "http",
      "auth": {
        "type": "bearer",
        "token": "${ECHO_RECEIVER_TOKEN}"
      },
      "timeout_ms": 30000,
      "retry": {
        "max_attempts": 3,
        "backoff_ms": 1000
      }
    }
  },
  "identity": {
    "provider": "stub",
    "config": {}
  },
  "runtime": {}
}
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://termin.dev/schemas/ir/0.9.0/appspec.json",
  "title": "Termin AppSpec IR",
  "description": "The complete Intermediate Representation of a compiled Termin application. Produced by the Termin compiler, consumed by any conforming Termin runtime.",
  "type": "object",
  "required": ["ir_version"],
  "additionalProperties": false,
  "properties": {
    "ir_version": {
      "type": "string",
      "const": "0.9.0",
      "description": "Semantic version of the IR schema. Runtimes should reject versions they don't support."
    },
    "reflection_enabled": {
      "type": "boolean",
      "default": true,
      "description": "Whether the runtime should expose Reflection endpoints (/__reflect/*) for operational visibility."
    },
    "app_id": {
      "type": ["string", "null"],
      "default": null,
      "description": "Globally unique application identifier (UUID). Generated by the compiler on first compile and written back to the source file. Used by deployment systems to match packages to existing deployments for upgrades vs new installs."
    },
    "name": {
      "type": "string",
      "description": "Human-readable application name from the DSL 'Application:' declaration."
    },
    "description": {
      "type": "string",
      "description": "Application description from the DSL 'Description:' declaration."
    },
    "auth": {
      "$ref": "#/$defs/AuthSpec",
      "description": "Authentication and authorization configuration. Null only for the simplest apps (anonymous access)."
    },
    "content": {
      "type": "array",
      "items": { "$ref": "#/$defs/ContentSchema" },
      "description": "All Content definitions — the data model. Each becomes a persistent storage table."
    },
    "access_grants": {
      "type": "array",
      "items": { "$ref": "#/$defs/AccessGrant" },
      "description": "Resolved access control rules: which scopes grant which verbs on which Content."
    },
    "state_machines": {
      "type": "array",
      "items": { "$ref": "#/$defs/StateMachineSpec" },
      "description": "State machines governing lifecycle transitions on Content (or Channels/Computes/Boundaries)."
    },
    "events": {
      "type": "array",
      "items": { "$ref": "#/$defs/EventSpec" },
      "description": "Event rules: triggers (CRUD or CEL conditions) that cause side-effects."
    },
    "routes": {
      "type": "array",
      "items": { "$ref": "#/$defs/RouteSpec" },
      "description": "Resolved HTTP API routes. Generated by the compiler from Content + AccessGrants."
    },
    "pages": {
      "type": "array",
      "items": { "$ref": "#/$defs/PageEntry" },
      "description": "Presentation layer: component trees defining the UI for each user story."
    },
    "nav_items": {
      "type": "array",
      "items": { "$ref": "#/$defs/NavItemSpec" },
      "description": "Navigation menu items with role-based visibility."
    },
    "streams": {
      "type": "array",
      "items": { "$ref": "#/$defs/StreamSpec" },
      "description": "Server-Sent Event stream definitions for real-time updates."
    },
    "computes": {
      "type": "array",
      "items": { "$ref": "#/$defs/ComputeSpec" },
      "description": "Compute modules: pure data transformations (Transform, Reduce, Expand, Correlate, Route)."
    },
    "channels": {
      "type": "array",
      "items": { "$ref": "#/$defs/ChannelSpec" },
      "description": "Channel definitions: declared data flow paths with direction and delivery guarantees."
    },
    "boundaries": {
      "type": "array",
      "items": { "$ref": "#/$defs/BoundarySpec" },
      "description": "Boundary definitions: trust and isolation zones grouping Content and other primitives."
    },
    "error_handlers": {
      "type": "array",
      "items": { "$ref": "#/$defs/ErrorHandlerSpec" },
      "description": "Error handling rules with retry, escalation, and notification actions."
    },
    "reclassification_points": {
      "type": "array",
      "items": { "$ref": "#/$defs/ReclassificationPoint" },
      "default": [],
      "description": "Declared points where data changes confidentiality level through a Compute module. The compiler extracts these from Compute definitions that have output_confidentiality_scope set. Runtimes use this list for audit logging and policy enforcement."
    },
    "required_contracts": {
      "type": "array",
      "items": { "type": "string" },
      "default": [],
      "description": "v0.9 Phase 5a.1: the set of fully-qualified presentation contracts the IR's pages require (e.g., 'presentation-base.page', 'presentation-base.data-table', 'airlock-components.cosmic-orb'). The runtime uses this for deploy-time validation per BRD #2 §8.5 — every contract listed here must have a bound provider in the deploy config (or a default-bound provider for the presentation-base namespace) before the app starts."
    }
  },

  "$defs": {

    "QualifiedName": {
      "type": "object",
      "description": "A resolved identifier with three forms. The compiler generates all three from the DSL display name. Runtime implementers should use 'snake' for storage/API keys and 'display' for UI rendering.",
      "required": ["display", "snake", "pascal"],
      "additionalProperties": false,
      "properties": {
        "display": {
          "type": "string",
          "description": "Original human-readable form: 'stock levels', 'order lines'."
        },
        "snake": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9_]*$",
          "description": "Snake_case form for storage tables, API paths, and internal keys: 'stock_levels'."
        },
        "pascal": {
          "type": "string",
          "pattern": "^[A-Z][a-zA-Z0-9]*$",
          "description": "PascalCase form for class names and type references: 'StockLevels'."
        }
      }
    },

    "FieldType": {
      "type": "string",
      "enum": ["TEXT", "INTEGER", "REAL", "BOOLEAN", "DATE", "TIMESTAMP", "JSON"],
      "description": "Storage-level column type. Maps to SQL column types in relational backends. TEXT is the default. JSON is used for list-typed fields."
    },

    "FieldSpec": {
      "type": "object",
      "description": "A single field in a Content schema. Combines the DSL's semantic business_type with a resolved storage column_type.",
      "required": ["name", "display_name"],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9_]*$",
          "description": "Snake_case field name, used as the storage column name and API key."
        },
        "display_name": {
          "type": "string",
          "description": "Original human-readable field name from the DSL: 'unit cost', 'parent order'."
        },
        "business_type": {
          "type": "string",
          "enum": ["text", "whole_number", "number", "currency", "percentage", "date", "datetime", "automatic", "boolean", "enum", "reference", "list", "state", "structured", "conversation"],
          "default": "text",
          "description": "Semantic type from the DSL. Runtimes use this for input validation, formatting, and UI hints. 'reference' means a foreign key to another Content. 'enum' means a constrained set of values. 'automatic' means a system-managed timestamp (created_at, updated_at). 'datetime' is a user-entered date+time. 'state' (v0.9+) means the field is a state machine — each state-typed field on a Content produces a corresponding StateMachineSpec entry; the column name matches the snake_case field name. 'structured' (v0.9.2 L1) is opaque JSON storage with CEL access (boundary cases like provider IO and opaque payloads, not app domain modeling). 'conversation' (v0.9.2 L2) is a JSON-array-backed field carrying canonical conversation entries — see conversation-field-contract.md for the entry shape and the Append CRUD verb."
        },
        "column_type": {
          "$ref": "#/$defs/FieldType",
          "default": "TEXT",
          "description": "Resolved storage type. The compiler maps business_type to column_type (e.g., currency -> REAL, whole_number -> INTEGER)."
        },
        "required": {
          "type": "boolean",
          "default": false,
          "description": "Whether the field must have a non-null value on create."
        },
        "unique": {
          "type": "boolean",
          "default": false,
          "description": "Whether the field value must be unique across all records."
        },
        "minimum": {
          "type": ["integer", "null"],
          "default": null,
          "description": "Minimum allowed value for numeric fields."
        },
        "maximum": {
          "type": ["integer", "null"],
          "default": null,
          "description": "Maximum allowed value for numeric fields."
        },
        "enum_values": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "Allowed values when business_type is 'enum'. Empty for non-enum fields."
        },
        "foreign_key": {
          "type": ["string", "null"],
          "default": null,
          "description": "Snake_case name of the target Content when business_type is 'reference'. The runtime creates a foreign key constraint to that Content's 'id' column."
        },
        "cascade_mode": {
          "type": ["string", "null"],
          "enum": ["cascade", "restrict", null],
          "default": null,
          "description": "v0.9+: cascade behavior on delete of the referenced parent. Required when business_type is 'reference'. 'cascade' = delete this row when the parent is deleted (ON DELETE CASCADE). 'restrict' = refuse parent delete if any rows reference it (ON DELETE RESTRICT). Null when business_type is not 'reference'. The compiler enforces this invariant statically (TERMIN-S039) and rejects transitive cascade-restrict deadlocks (TERMIN-S042) and multi-content cascade cycles (TERMIN-S043) before any IR is emitted."
        },
        "is_auto": {
          "type": "boolean",
          "default": false,
          "description": "Whether the field is automatically populated (e.g., 'created_at' timestamps). Auto fields are excluded from create/update forms."
        },
        "list_type": {
          "type": ["string", "null"],
          "default": null,
          "description": "Inner type name when business_type is 'list'. Stored as JSON array in a JSON column."
        },
        "default_expr": {
          "type": ["string", "null"],
          "default": null,
          "description": "Default value expression evaluated at record creation time. If from a CEL expression (DSL: defaults to [expr]), this is the raw expression string (e.g., 'the_user.display_name', '0', 'now'). If from a literal (DSL: defaults to \"text\"), this is a CEL string literal (e.g., '\"normal\"'). The runtime evaluates this with the_user identity binding (per BRD #3 §4.2) and system variables (now, today) in scope. Null means no default — the field is left empty unless provided by the caller."
        },
        "confidentiality_scopes": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "Confidentiality labels attached to this field. Data in this field is only accessible to identities holding all listed scopes. Empty means unrestricted within the Content's own access grants."
        },
        "one_of_values": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "Field-level constraint: value must be one of the listed strings. Empty means no constraint. Unified with enum_values — one_of can apply to any base type, not just enums."
        }
      },
      "allOf": [
        {
          "description": "v0.9+ cascade invariant: cascade_mode is required exactly when business_type is 'reference'.",
          "if": {
            "properties": { "business_type": { "const": "reference" } },
            "required": ["business_type"]
          },
          "then": {
            "properties": {
              "cascade_mode": {
                "type": "string",
                "enum": ["cascade", "restrict"]
              }
            },
            "required": ["cascade_mode"]
          },
          "else": {
            "properties": {
              "cascade_mode": { "type": "null" }
            }
          }
        }
      ]
    },

    "ContentSchema": {
      "type": "object",
      "description": "A Content definition — the fundamental data primitive. Each ContentSchema becomes a persistent storage table with an implicit auto-increment 'id' primary key. State-typed fields produce additional columns named after the field (snake_case) and enforce transitions per the corresponding StateMachineSpec.",
      "required": ["name", "fields"],
      "additionalProperties": false,
      "properties": {
        "name": { "$ref": "#/$defs/QualifiedName" },
        "singular": {
          "type": "string",
          "default": "",
          "description": "Authoritative singular form from the DSL (e.g., 'echo' for Content 'echoes'). Derived from 'Each X has...' declarations. Used by the runtime for event context naming."
        },
        "fields": {
          "type": "array",
          "items": { "$ref": "#/$defs/FieldSpec" },
          "description": "Declared fields. The runtime adds implicit 'id' (auto-increment PK). State-typed fields are represented as normal FieldSpec entries (business_type='state'); their column name is the snake_case field name."
        },
        "storage_intent": {
          "type": "string",
          "enum": ["auto", "persistent", "ephemeral"],
          "default": "auto",
          "description": "Storage hint. 'auto' lets the runtime choose (typically persistent). Future: 'ephemeral' for in-memory-only Content."
        },
        "state_machines": {
          "type": "array",
          "default": [],
          "description": "v0.9: one entry per state-typed field on this Content. machine_name is the snake_case field name (also the SQL column name); initial is the state assigned on create. Multiple entries are allowed — a Content may own several state machines, one per state-typed field.",
          "items": {
            "type": "object",
            "required": ["machine_name", "initial"],
            "additionalProperties": false,
            "properties": {
              "machine_name": { "type": "string" },
              "initial":      { "type": "string" }
            }
          }
        },
        "confidentiality_scopes": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "Default confidentiality scopes applied to all records in this Content. Individual fields may declare additional scopes. Empty means no Content-level confidentiality restriction."
        },
        "audit": {
          "type": "string",
          "enum": ["actions", "debug", "none"],
          "default": "actions",
          "description": "Audit level for this Content. 'actions' (default): log event type, record ID, field names, identity, timestamp — never field values. 'debug': log full field values (opt-in, development only). 'none': suppress logging entirely."
        },
        "dependent_values": {
          "type": "array",
          "items": { "$ref": "#/$defs/DependentValueSpec" },
          "default": [],
          "description": "Conditional constraints on field values. When a condition holds, another field is constrained to specific values, required to equal a value, or given a default."
        },
        "ownership": {
          "type": ["object", "null"],
          "default": null,
          "description": "v0.9 Phase 6a: ownership cascade declaration. When present, identifies the field that links each row to its owning Principal — the runtime applies row-level ownership filters when access rules carry the `their own` qualifier. Shape: {field: <field-name>, references: <content-ref>}."
        },
        "audit_level": {
          "type": ["string", "null"],
          "default": null,
          "description": "v0.9 Phase 3 slice (d): audit level for compute-audit-log content tables (`compute_audit_log_<compute>`). One of 'actions' (default), 'debug', or 'none'. Mirrors `audit` for compute audit tables; nullable for non-audit content."
        },
        "audit_content_ref": {
          "type": ["string", "null"],
          "default": null,
          "description": "v0.9 Phase 3 slice (d): for compute-audit-log content, the snake_case name of the audit table itself (always 'compute_audit_log_<compute_snake>'). Helps the runtime distinguish synthesized audit content from app-author-declared content."
        }
      }
    },

    "DependentValueSpec": {
      "type": "object",
      "description": "A conditional constraint: When [condition], [field] must be/defaults to [value(s)].",
      "required": ["field", "constraint"],
      "additionalProperties": false,
      "properties": {
        "when": {
          "type": ["string", "null"],
          "default": null,
          "description": "CEL condition expression. Null means unconditional constraint."
        },
        "field": {
          "type": "string",
          "description": "Snake_case field name this constraint applies to."
        },
        "constraint": {
          "type": "string",
          "enum": ["one_of", "equals", "default"],
          "description": "Constraint type: 'one_of' restricts to allowed values, 'equals' requires exact match, 'default' sets a default if field not provided."
        },
        "values": {
          "type": "array",
          "items": {},
          "default": [],
          "description": "Allowed values for 'one_of' constraint."
        },
        "value": {
          "description": "Target value for 'equals' or 'default' constraint.",
          "default": null
        }
      }
    },

    "Verb": {
      "type": "string",
      "enum": ["VIEW", "CREATE", "UPDATE", "DELETE", "AUDIT", "APPEND"],
      "description": "CRUD+Audit operation verb for access control. AUDIT gates read access to execution traces (D-20). APPEND (v0.9.2 L3) is a field-targeted permission for conversation fields — append_field on the grant identifies the target."
    },

    "AccessGrant": {
      "type": "object",
      "description": "A resolved access control rule: 'Anyone with scope X can perform verbs Y on Content Z.'",
      "required": ["content", "scope", "verbs"],
      "additionalProperties": false,
      "properties": {
        "content": {
          "type": "string",
          "description": "Snake_case Content name this grant applies to."
        },
        "scope": {
          "type": "string",
          "description": "The scope string required. Must match a scope declared in auth.scopes."
        },
        "verbs": {
          "type": "array",
          "items": { "$ref": "#/$defs/Verb" },
          "minItems": 1,
          "description": "Which operations this scope grants."
        },
        "their_own": {
          "type": "boolean",
          "default": false,
          "description": "v0.9 Phase 6a: when true, this grant's verbs apply only to records the principal owns (per the Content's `ownership` declaration). Source form: `Anyone with X can update their own Y`."
        },
        "append_field": {
          "type": ["string", "null"],
          "default": null,
          "description": "v0.9.2 L3: when verbs contains APPEND, names the snake_case conversation field this grant targets. Source form: `Anyone with X can append to Y' <field>`. Null for non-APPEND grants."
        }
      }
    },

    "AuthSpec": {
      "type": "object",
      "description": "Authentication configuration. The 'stub' provider is a development-mode cookie-based auth. Production runtimes should implement pluggable providers (SSO, OIDC, etc.).",
      "required": ["provider", "scopes", "roles"],
      "additionalProperties": false,
      "properties": {
        "provider": {
          "type": "string",
          "description": "Auth provider identifier. 'stub' = built-in dev auth with role picker."
        },
        "scopes": {
          "type": "array",
          "items": { "type": "string" },
          "description": "All declared scopes in the application. Scopes are flat strings — not hierarchical."
        },
        "roles": {
          "type": "array",
          "items": { "$ref": "#/$defs/RoleSpec" },
          "description": "Named role definitions, each mapping to a set of scopes."
        }
      }
    },

    "RoleSpec": {
      "type": "object",
      "description": "A named role that bundles scopes for assignment to identities.",
      "required": ["name", "scopes"],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "description": "Role name from the DSL: 'warehouse manager', 'order clerk'."
        },
        "scopes": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Scopes this role grants."
        }
      }
    },

    "TransitionFeedbackSpec": {
      "type": "object",
      "description": "Feedback notification shown to the user after a state transition succeeds or fails.",
      "required": ["trigger", "style", "message", "is_expr"],
      "additionalProperties": false,
      "properties": {
        "trigger": {
          "type": "string",
          "enum": ["success", "error"],
          "description": "When to show this feedback: on successful transition or on failure."
        },
        "style": {
          "type": "string",
          "enum": ["toast", "banner"],
          "description": "Notification style. Toast: auto-dismiss (default 5s), bottom-right. Banner: persistent, stackable, inline."
        },
        "message": {
          "type": "string",
          "description": "Message text. If is_expr is true, this is a CEL expression with {singular}.*, from_state, to_state, and User.* in scope."
        },
        "is_expr": {
          "type": "boolean",
          "description": "True if message is a CEL expression (backtick-delimited in DSL), false if literal string."
        },
        "dismiss_seconds": {
          "type": ["integer", "null"],
          "description": "Auto-dismiss timer in seconds. Null means use type default (toast: 5s, banner: no auto-dismiss)."
        }
      }
    },

    "TransitionSpec": {
      "type": "object",
      "description": "A single allowed state transition, with a required scope and optional user feedback.",
      "required": ["from_state", "to_state", "required_scope"],
      "additionalProperties": false,
      "properties": {
        "from_state": { "type": "string" },
        "to_state": { "type": "string" },
        "required_scope": {
          "type": "string",
          "description": "Scope the caller's identity must hold to perform this transition."
        },
        "feedback": {
          "type": "array",
          "items": { "$ref": "#/$defs/TransitionFeedbackSpec" },
          "description": "Optional notification feedback to show the user after this transition. Empty array if no feedback declared."
        }
      }
    },

    "StateMachineSpec": {
      "type": "object",
      "description": "A state machine governing lifecycle transitions. The runtime must enforce that only declared transitions are allowed, and only when the caller holds the required scope.",
      "required": ["content_ref", "machine_name", "initial_state", "states", "transitions"],
      "additionalProperties": false,
      "properties": {
        "content_ref": {
          "type": "string",
          "description": "Snake_case name of the Content (or Channel/Compute/Boundary) this machine governs."
        },
        "machine_name": {
          "type": "string",
          "description": "Snake-case field name. Also used as the SQL column name by conforming runtimes — one column per state machine on the owning Content. A Content may declare multiple state-typed fields, each producing its own StateMachineSpec with a distinct machine_name."
        },
        "initial_state": {
          "type": "string",
          "description": "State assigned to new records on creation."
        },
        "states": {
          "type": "array",
          "items": { "type": "string" },
          "minItems": 1,
          "description": "All valid states. Multi-word states are lowercase with spaces: 'in progress', 'under review'."
        },
        "transitions": {
          "type": "array",
          "items": { "$ref": "#/$defs/TransitionSpec" },
          "description": "Allowed transitions. Any transition not listed here is forbidden."
        },
        "primitive_type": {
          "type": "string",
          "enum": ["content", "channel", "compute", "boundary"],
          "default": "content",
          "description": "Which primitive type this state machine is attached to. Usually 'content'."
        }
      }
    },

    "EventConditionSpec": {
      "type": "object",
      "description": "A simple column comparison condition for legacy event triggers.",
      "required": ["left_column", "operator", "right_column"],
      "additionalProperties": false,
      "properties": {
        "left_column": { "type": "string", "description": "Snake_case column name." },
        "operator": {
          "type": "string",
          "enum": ["lte", "gte", "eq"],
          "description": "Comparison operator."
        },
        "right_column": { "type": "string", "description": "Snake_case column name to compare against." }
      }
    },

    "EventActionSpec": {
      "type": "object",
      "description": "Action to take when an event fires: create a record in target Content, or send data to a Channel.",
      "additionalProperties": false,
      "properties": {
        "target_content": {
          "type": "string",
          "default": "",
          "description": "Snake_case Content name to create a record in (for create actions)."
        },
        "column_mapping": {
          "type": "array",
          "items": {
            "type": "array",
            "items": { "type": "string" },
            "minItems": 2,
            "maxItems": 2
          },
          "default": [],
          "description": "Array of [target_column, source_column] pairs (for create actions)."
        },
        "send_content": {
          "type": "string",
          "default": "",
          "description": "Content/record to send (for Channel send actions)."
        },
        "send_channel": {
          "type": "string",
          "default": "",
          "description": "Channel name to send data to (for Channel send actions)."
        }
      }
    },

    "EventSpec": {
      "type": "object",
      "description": "An event rule that fires side-effects when Content changes. Events are the reactive glue between Content types.",
      "required": ["source_content", "trigger"],
      "additionalProperties": false,
      "properties": {
        "source_content": {
          "type": "string",
          "description": "Snake_case Content name that triggers this event."
        },
        "trigger": {
          "type": "string",
          "enum": ["created", "updated", "deleted", "expr"],
          "description": "What triggers the event. 'expr' uses the condition_expr expression."
        },
        "condition": {
          "oneOf": [
            { "$ref": "#/$defs/EventConditionSpec" },
            { "type": "null" }
          ],
          "default": null,
          "description": "Legacy simple condition (column comparison). Null for unconditional or CEL triggers."
        },
        "action": {
          "oneOf": [
            { "$ref": "#/$defs/EventActionSpec" },
            { "type": "null" }
          ],
          "default": null,
          "description": "Action to perform when the event fires."
        },
        "condition_expr": {
          "type": ["string", "null"],
          "default": null,
          "description": "CEL expression evaluated against the record. Event fires when this evaluates to truthy."
        },
        "log_level": {
          "type": "string",
          "enum": ["TRACE", "DEBUG", "INFO", "WARN", "ERROR"],
          "default": "INFO",
          "description": "Severity level for event bus logging."
        }
      }
    },

    "HttpMethod": {
      "type": "string",
      "enum": ["GET", "POST", "PUT", "DELETE"]
    },

    "RouteKind": {
      "type": "string",
      "enum": ["LIST", "GET_ONE", "CREATE", "UPDATE", "DELETE", "TRANSITION", "STREAM", "APPEND"],
      "description": "The semantic purpose of a route. Runtimes use this to select the correct handler pattern. APPEND (v0.9.2 L3) is a field-targeted action route at POST /<resource>/{id}/<field>:append — appends a new entry to a conversation field; field_name on the spec names the target column."
    },

    "RouteSpec": {
      "type": "object",
      "description": "A resolved HTTP API route. The compiler generates routes from Content + AccessGrants. Runtimes may also generate routes dynamically from ContentSchema if this array is empty.",
      "required": ["method", "path", "kind", "content_ref"],
      "additionalProperties": false,
      "properties": {
        "method": { "$ref": "#/$defs/HttpMethod" },
        "path": {
          "type": "string",
          "description": "URL path pattern. Parameters use {name} syntax: '/api/v1/products/{id}'."
        },
        "kind": { "$ref": "#/$defs/RouteKind" },
        "content_ref": {
          "type": "string",
          "description": "Snake_case Content name this route operates on."
        },
        "required_scope": {
          "type": ["string", "null"],
          "default": null,
          "description": "Scope the caller must hold. Null means no scope check (public route)."
        },
        "lookup_column": {
          "type": "string",
          "default": "id",
          "description": "Column used for {param} in the path. Usually 'id'."
        },
        "target_state": {
          "type": ["string", "null"],
          "default": null,
          "description": "For TRANSITION routes: the target state to transition to."
        },
        "machine_name": {
          "type": ["string", "null"],
          "default": null,
          "description": "v0.9: For TRANSITION routes on multi-state-machine Content, the snake_case state-machine identifier this route drives. Same value as the corresponding StateMachineSpec.machine_name and the underlying state-typed field's snake_case name. Two different state machines on the same Content may share a target_state name (e.g. both lifecycle and approval_status could declare an 'approved' state); machine_name is the disambiguator. Null on legacy single-SM IRs and on non-TRANSITION routes."
        },
        "row_filter": {
          "type": ["object", "null"],
          "default": null,
          "description": "v0.9 Phase 6a: row-level filter shape. When present, the runtime applies this filter as an implicit WHERE clause for routes targeting Content with `their own` access rules — restricts results to records the principal owns. Schema is provider-agnostic; populated at lower time from the access rule."
        },
        "field_name": {
          "type": ["string", "null"],
          "default": null,
          "description": "v0.9.2 L3: For APPEND routes, the snake_case conversation field name this route appends to. Encoded in the path AND on the spec so handlers locate the JSON column without re-parsing the path. Null on non-APPEND routes."
        }
      }
    },

    "PropValue": {
      "type": "object",
      "description": "A CEL expression prop. When is_expr is true, the runtime must evaluate the value as a CEL expression against the current data context.",
      "required": ["value", "is_expr"],
      "additionalProperties": false,
      "properties": {
        "value": { "type": "string" },
        "is_expr": { "type": "boolean", "const": true }
      }
    },

    "ComponentNode": {
      "type": "object",
      "description": "A composable UI component in the Presentation tree. Props vary by component type — see the Runtime Implementer's Guide for per-type prop schemas.",
      "required": ["type", "props"],
      "additionalProperties": false,
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "text", "data_table", "chat", "form", "field_input", "section",
            "aggregation", "stat_breakdown", "chart",
            "filter", "search", "highlight", "subscribe",
            "related", "action_button", "semantic_mark",
            "edit_modal", "package_contract"
          ],
          "description": "Component type. Determines which props are expected and how the component renders. v0.9 Phase 5c.2 adds 'package_contract' for instances of contract-package source-verbs."
        },
        "contract": {
          "type": "string",
          "default": "",
          "description": "v0.9 Phase 5a.1: fully-qualified presentation contract this node renders against (e.g., 'presentation-base.data-table', 'airlock-components.cosmic-orb'). Empty means use the default contract for `type`. The runtime resolves provider bindings against this name; deploy-time validation aggregates these into AppSpec.required_contracts."
        },
        "props": {
          "type": "object",
          "description": "Component-specific properties. Values are either plain strings/numbers/booleans/arrays, or PropValue objects for CEL expressions. See the Implementer's Guide for per-type prop documentation."
        },
        "style": {
          "type": "object",
          "default": {},
          "description": "CSS-like visual properties. Reserved for visual editor use. Runtimes may ignore or apply these."
        },
        "layout": {
          "type": "object",
          "default": {},
          "description": "Canvas positioning for the visual editor. Runtimes typically ignore this for rendered output."
        },
        "children": {
          "type": "array",
          "items": { "$ref": "#/$defs/ComponentNode" },
          "default": [],
          "description": "Child components. Nesting rules: data_table can contain filter/search/highlight/subscribe/related. form can contain field_input. section can contain anything."
        }
      }
    },

    "PageEntry": {
      "type": "object",
      "description": "A page in the Presentation layer. Each page corresponds to a user story in the DSL and contains a component tree describing the UI.",
      "required": ["name", "slug", "role"],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "description": "Human-readable page title: 'Inventory Dashboard'."
        },
        "slug": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9_]*$",
          "description": "URL-safe page identifier: 'inventory_dashboard'. Used in route paths."
        },
        "role": {
          "type": "string",
          "description": "The role this page belongs to, or 'anonymous' for public pages."
        },
        "required_scope": {
          "type": ["string", "null"],
          "default": null,
          "description": "Scope required to access this page. Null means role membership is sufficient."
        },
        "children": {
          "type": "array",
          "items": { "$ref": "#/$defs/ComponentNode" },
          "default": [],
          "description": "Top-level components on this page."
        }
      }
    },

    "NavItemSpec": {
      "type": "object",
      "description": "A navigation menu entry. Runtimes render these as the app's main navigation.",
      "required": ["label", "page_slug", "visible_to"],
      "additionalProperties": false,
      "properties": {
        "label": {
          "type": "string",
          "description": "Display text in the navigation menu."
        },
        "page_slug": {
          "type": "string",
          "description": "Slug of the target PageEntry."
        },
        "visible_to": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Role names that can see this nav item. ['all'] means visible to everyone."
        },
        "badge_content": {
          "type": ["string", "null"],
          "default": null,
          "description": "Snake_case Content name. If set, the nav item shows a count badge (SELECT COUNT(*) from that Content)."
        }
      }
    },

    "StreamSpec": {
      "type": "object",
      "description": "A Server-Sent Event stream endpoint for real-time push updates.",
      "required": ["description", "path"],
      "additionalProperties": false,
      "properties": {
        "description": { "type": "string" },
        "path": {
          "type": "string",
          "description": "URL path for the SSE endpoint: '/api/v1/stream/products'."
        }
      }
    },

    "ComputeShape": {
      "type": "string",
      "enum": ["NONE", "TRANSFORM", "REDUCE", "EXPAND", "CORRELATE", "ROUTE"],
      "description": "The data flow shape of a Compute module. NONE: no shape (LLM/agent providers use field wiring instead). TRANSFORM: 1-to-1. REDUCE: many-to-1. EXPAND: 1-to-many. CORRELATE: many-to-many join. ROUTE: 1-to-conditional-output."
    },

    "ComputeParamSpec": {
      "type": "object",
      "description": "A typed parameter for Compute input or output.",
      "required": ["name", "type_name"],
      "additionalProperties": false,
      "properties": {
        "name": { "type": "string" },
        "type_name": { "type": "string" }
      }
    },

    "ComputeSpec": {
      "type": "object",
      "description": "A Compute module — a data transformation, LLM completion, or autonomous agent. CEL Computes use Transform shapes with CEL bodies. LLM Computes use field wiring (input_fields/output_fields). Agent Computes use the ComputeContext tool API scoped to their Accesses declaration.",
      "required": ["name", "shape"],
      "additionalProperties": false,
      "properties": {
        "name": { "$ref": "#/$defs/QualifiedName" },
        "shape": { "$ref": "#/$defs/ComputeShape" },
        "input_content": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Snake_case Content names this Compute reads from."
        },
        "output_content": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Snake_case Content names this Compute writes to."
        },
        "body_lines": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "CEL expression lines forming the Compute body. Evaluated server-side (or client-side if client_safe)."
        },
        "required_scope": {
          "type": ["string", "null"],
          "default": null,
          "description": "Scope required to invoke this Compute."
        },
        "required_role": {
          "type": ["string", "null"],
          "default": null,
          "description": "Alternative to required_scope: role name required to invoke."
        },
        "input_params": {
          "type": "array",
          "items": { "$ref": "#/$defs/ComputeParamSpec" },
          "default": [],
          "description": "Typed input parameters beyond Content references."
        },
        "output_params": {
          "type": "array",
          "items": { "$ref": "#/$defs/ComputeParamSpec" },
          "default": [],
          "description": "Typed output parameters beyond Content references."
        },
        "client_safe": {
          "type": "boolean",
          "default": false,
          "description": "Compiler-inferred flag: true when the Compute's CEL body can be safely evaluated on the client (no server-only field access, no side effects). Client runtimes may use this for optimistic local evaluation."
        },
        "identity_mode": {
          "type": "string",
          "enum": ["delegate", "service"],
          "default": "delegate",
          "description": "Identity propagation mode. 'delegate': the Compute executes with the caller's identity and scopes. 'service': the Compute executes with its own service identity, decoupled from the caller."
        },
        "required_confidentiality_scopes": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "Confidentiality scopes the caller must hold for all input data consumed by this Compute. The runtime must verify the caller's identity covers these scopes before execution."
        },
        "output_confidentiality_scope": {
          "type": ["string", "null"],
          "default": null,
          "description": "Confidentiality scope stamped on data produced by this Compute. Null means the output inherits scopes from the input data. Non-null enables reclassification — the Compute transforms data from one confidentiality level to another."
        },
        "field_dependencies": {
          "type": "array",
          "items": { "$ref": "#/$defs/FieldDependency" },
          "default": [],
          "description": "Explicit field-level data dependencies. Declares which fields from which Content this Compute reads, along with the confidentiality scopes required to access them."
        },
        "provider": {
          "type": ["string", "null"],
          "default": null,
          "description": "Compute provider. null or 'cel' (default CEL evaluator), 'llm' (Level 1 field-to-field completion), 'ai-agent' (Level 3 autonomous AI with tools)."
        },
        "preconditions": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "CEL expressions evaluated before execution. All must return true or execution is rejected (HTTP 412)."
        },
        "postconditions": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "CEL expressions evaluated after execution against the staging snapshot. All must return true or the transaction is rolled back (HTTP 409)."
        },
        "directive": {
          "type": ["string", "null"],
          "default": null,
          "description": "System prompt (strong prior) for LLM/agent providers. The agent's identity, rules, and constraints. Maps to the LLM system message."
        },
        "objective": {
          "type": ["string", "null"],
          "default": null,
          "description": "Task prompt for LLM/agent providers. What to accomplish in this invocation. For agents, includes the strategy (tool usage plan). May contain inline CEL expressions."
        },
        "directive_source": {
          "type": ["object", "null"],
          "default": null,
          "description": "v0.9 Phase 6c (BRD #3 §6.2): non-inline Directive sourcing. When set, `directive` is empty until resolved. Two shapes — `{kind: \"deploy_config\", key: <string>}` (resolved at app startup from deploy_config[<key>]) or `{kind: \"field\", content: <string>, field: <string>}` (resolved from the triggering record at each invocation).",
          "oneOf": [
            { "type": "null" },
            {
              "type": "object",
              "properties": {
                "kind": { "type": "string", "enum": ["deploy_config", "field"] },
                "key": { "type": "string" },
                "content": { "type": "string" },
                "field": { "type": "string" }
              },
              "required": ["kind"]
            }
          ]
        },
        "objective_source": {
          "type": ["object", "null"],
          "default": null,
          "description": "v0.9 Phase 6c (BRD #3 §6.3): non-inline Objective sourcing. Same two shapes as directive_source.",
          "oneOf": [
            { "type": "null" },
            {
              "type": "object",
              "properties": {
                "kind": { "type": "string", "enum": ["deploy_config", "field"] },
                "key": { "type": "string" },
                "content": { "type": "string" },
                "field": { "type": "string" }
              },
              "required": ["kind"]
            }
          ]
        },
        "strategy": {
          "type": ["string", "null"],
          "default": null,
          "description": "Legacy: execution plan for agents. Folded into Objective in v0.5.0+. Retained for backward compatibility."
        },
        "trigger": {
          "type": ["string", "null"],
          "default": null,
          "description": "Trigger mechanism: 'schedule <interval>' or 'event \"<name>\"'. null means API-invoked only."
        },
        "trigger_where": {
          "type": ["string", "null"],
          "default": null,
          "description": "CEL expression for event trigger filtering. When present, the Compute is only invoked if this expression evaluates to true against the triggering event. Routing filter, not a safety gate (see preconditions for that)."
        },
        "conversation_source": {
          "type": ["array", "null"],
          "default": null,
          "items": { "type": "string" },
          "minItems": 2,
          "maxItems": 2,
          "description": "v0.9.2 L6 (compiler tech design §10): wired conversation source for ai-agent computes. [content_snake_name, field_name] when present. The runtime materializes the conversation field at invocation time, passes it as native LLM context to the provider, and auto-appends the assistant entry on response. Mutually exclusive with `accesses` of the same content (TERMIN-S057). The compute must trigger on the matching `<content>.<field>.appended` event (TERMIN-S058)."
        },
        "accesses": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "Content types this Compute can access (resolved snake_case names). Required boundary declaration. The runtime restricts agent tool calls to these content types. The compiler cross-checks field wiring and CEL body references against this list."
        },
        "reads": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "v0.9 Phase 3 slice (c): read-only Content access. Subset of `Accesses` semantics — the agent can query these but not write."
        },
        "sends_to": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "v0.9 Phase 3 slice (c): outbound Channel names this Compute may dispatch to."
        },
        "emits": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "v0.9 Phase 3 slice (c): event names this Compute may emit on the runtime event bus."
        },
        "invokes": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "v0.9 Phase 3 slice (c): other Compute names this Compute may invoke."
        },
        "input_fields": {
          "type": "array",
          "items": {
            "type": "array",
            "items": { "type": "string" },
            "minItems": 2,
            "maxItems": 2
          },
          "default": [],
          "description": "Field wiring for LLM input. Array of [content_ref, field_name] pairs. The runtime reads these fields from the triggering record and passes them to the LLM as input context."
        },
        "output_fields": {
          "type": "array",
          "items": {
            "type": "array",
            "items": { "type": "string" },
            "minItems": 2,
            "maxItems": 2
          },
          "default": [],
          "description": "Field wiring for LLM output. Array of [content_ref, field_name] pairs. The runtime writes the LLM response into these fields on the triggering record."
        },
        "output_creates": {
          "type": ["string", "null"],
          "default": null,
          "description": "Content type name for 'Output creates X'. When set, the LLM response creates a new record in this content type instead of updating the triggering record."
        },
        "audit_level": {
          "type": "string",
          "enum": ["none", "actions", "debug"],
          "default": "actions",
          "description": "D-20: Audit level for compute traces. 'none': no trace. 'actions' (default): outcome + what the compute did. 'debug': everything including system prompt and thinking."
        },
        "audit_scope": {
          "type": ["string", "null"],
          "default": null,
          "description": "D-20: Scope string from 'Anyone with X can audit'. Gates AUDIT access to this compute's trace log."
        },
        "audit_content_ref": {
          "type": ["string", "null"],
          "default": null,
          "description": "D-20: Snake_case name of the auto-generated audit log Content table (compute_audit_log_{name}). Null when audit_level is 'none'."
        }
      }
    },

    "ChannelDirection": {
      "type": "string",
      "enum": ["INBOUND", "OUTBOUND", "BIDIRECTIONAL", "INTERNAL"],
      "description": "Data flow direction. INBOUND: external -> app. OUTBOUND: app -> external. BIDIRECTIONAL: both. INTERNAL: within-boundary only (never crosses a Boundary)."
    },

    "ChannelDelivery": {
      "type": "string",
      "enum": ["REALTIME", "RELIABLE", "BATCH", "AUTO"],
      "description": "Delivery guarantee intent. REALTIME: WebSocket/SSE, may drop. RELIABLE: persistent queue, at-least-once. BATCH: periodic bulk. AUTO: runtime decides based on volume."
    },

    "ChannelRequirementSpec": {
      "type": "object",
      "description": "A scope requirement on a Channel endpoint: who can send, receive, or invoke on this Channel.",
      "required": ["scope", "direction"],
      "additionalProperties": false,
      "properties": {
        "scope": { "type": "string" },
        "direction": {
          "type": "string",
          "enum": ["send", "receive", "invoke"],
          "description": "Whether this scope is required for sending, receiving, or invoking actions."
        }
      }
    },

    "ChannelActionParamSpec": {
      "type": "object",
      "description": "A typed parameter on a Channel Action (input or output).",
      "required": ["name", "param_type"],
      "additionalProperties": false,
      "properties": {
        "name": { "type": "string", "description": "Parameter name." },
        "param_type": { "type": "string", "description": "Type: text, number, yes or no, or a Content name." }
      }
    },

    "ChannelActionSpec": {
      "type": "object",
      "description": "A typed RPC verb exposed by a Channel. Actions have typed inputs, typed outputs, and scope requirements. They represent side-effectful operations on external services.",
      "required": ["name"],
      "additionalProperties": false,
      "properties": {
        "name": { "$ref": "#/$defs/QualifiedName" },
        "takes": {
          "type": "array",
          "items": { "$ref": "#/$defs/ChannelActionParamSpec" },
          "default": [],
          "description": "Input parameters for this action."
        },
        "returns": {
          "type": "array",
          "items": { "$ref": "#/$defs/ChannelActionParamSpec" },
          "default": [],
          "description": "Output parameters from this action."
        },
        "required_scopes": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "Scopes required to invoke this action."
        }
      }
    },

    "ChannelSpec": {
      "type": "object",
      "description": "A Channel — a declared data flow path with optional typed actions. Channels make data movement explicit and enforceable. Data Channels carry Content records. Action Channels expose typed RPC verbs to external services. A Channel can do both.",
      "required": ["name"],
      "additionalProperties": false,
      "properties": {
        "name": { "$ref": "#/$defs/QualifiedName" },
        "carries_content": {
          "type": "string",
          "default": "",
          "description": "Snake_case Content name describing the shape of data this Channel transports. Empty for action-only Channels."
        },
        "direction": {
          "$ref": "#/$defs/ChannelDirection",
          "default": "INBOUND"
        },
        "delivery": {
          "$ref": "#/$defs/ChannelDelivery",
          "default": "AUTO"
        },
        "endpoint": {
          "type": ["string", "null"],
          "default": null,
          "description": "Explicit endpoint path. Deprecated — use deployment config instead."
        },
        "requirements": {
          "type": "array",
          "items": { "$ref": "#/$defs/ChannelRequirementSpec" },
          "default": [],
          "description": "Scope requirements for sending/receiving on this Channel."
        },
        "actions": {
          "type": "array",
          "items": { "$ref": "#/$defs/ChannelActionSpec" },
          "default": [],
          "description": "Typed RPC verbs this Channel exposes. Actions have typed I/O and scope requirements."
        },
        "provider_contract": {
          "type": ["string", "null"],
          "default": null,
          "description": "v0.9 Phase 4: name of the provider contract this Channel binds to (e.g., 'webhook', 'email', 'messaging', 'event-stream'). Null for internal / legacy URL channels with no provider declaration. Deploy config's `bindings.channels.<name>.provider` must register a product against this contract."
        },
        "failure_mode": {
          "type": "string",
          "enum": ["log-and-drop", "surface-as-error", "queue-and-retry"],
          "default": "log-and-drop",
          "description": "Dispatch failure semantics. `log-and-drop` (default) never raises; the send() exception is logged and the result carries outcome=failed. `surface-as-error` propagates the exception to the caller as ChannelError (v0.9.1 reference runtime implements this). `queue-and-retry` enqueues the payload for redelivery with exponential backoff; after a configurable max retry window (default reasonable, max 24h) the payload moves to a dead-letter table — full implementation lands v0.10, grammar accepted in v0.9.x with log-and-drop fallback."
        }
      }
    },

    "BoundaryPropertySpec": {
      "type": "object",
      "description": "A computed property exposed by a Boundary. Queryable via Reflection.",
      "required": ["name", "type_name", "expr"],
      "additionalProperties": false,
      "properties": {
        "name": { "type": "string" },
        "type_name": { "type": "string" },
        "expr": {
          "type": "string",
          "description": "CEL expression evaluated against the Boundary's contained Content."
        }
      }
    },

    "BoundarySpec": {
      "type": "object",
      "description": "A Boundary — a trust and isolation zone. Boundaries group Content, enforce identity policies, and control data flow via Channels. Data can only cross a Boundary through declared Channels.",
      "required": ["name"],
      "additionalProperties": false,
      "properties": {
        "name": { "$ref": "#/$defs/QualifiedName" },
        "boundary_type": {
          "type": "string",
          "enum": ["application", "library", "module", "configuration"],
          "default": "application",
          "description": "The kind of Boundary. 'application' is a top-level deployable unit. 'library' provides reusable Content/Compute/Channel definitions. 'module' is a sub-unit within an application. 'configuration' holds settings."
        },
        "contains_content": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "Snake_case Content names contained in this Boundary."
        },
        "contains_boundaries": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "Snake_case names of child Boundaries (nesting)."
        },
        "identity_mode": {
          "type": "string",
          "enum": ["inherit", "restrict"],
          "default": "inherit",
          "description": "'inherit': caller identity flows through unchanged. 'restrict': only the listed identity_scopes are allowed within this Boundary."
        },
        "identity_scopes": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "Scopes allowed within this Boundary when identity_mode is 'restrict'."
        },
        "properties": {
          "type": "array",
          "items": { "$ref": "#/$defs/BoundaryPropertySpec" },
          "default": [],
          "description": "Computed properties exposed by this Boundary for Reflection queries."
        }
      }
    },

    "ErrorActionSpec": {
      "type": "object",
      "description": "An action taken in response to an error: retry, disable, escalate, create a record, notify, or set a value.",
      "required": ["kind"],
      "additionalProperties": false,
      "properties": {
        "kind": {
          "type": "string",
          "enum": ["retry", "disable", "escalate", "create", "notify", "set"],
          "description": "Action type. 'retry': re-attempt with optional backoff. 'disable': stop the erroring primitive. 'escalate': promote severity. 'create': create a record. 'notify': send notification. 'set': set a field value."
        },
        "retry_count": {
          "type": "integer",
          "default": 0,
          "description": "Number of retry attempts (for kind='retry')."
        },
        "retry_backoff": {
          "type": "boolean",
          "default": false,
          "description": "Whether to use exponential backoff between retries."
        },
        "retry_max_delay": {
          "type": ["string", "null"],
          "default": null,
          "description": "Maximum delay between retries (e.g., '30s', '5m')."
        },
        "target": {
          "type": ["string", "null"],
          "default": null,
          "description": "Target Content or Channel for create/notify actions."
        },
        "expr": {
          "type": ["string", "null"],
          "default": null,
          "description": "CEL expression for computed values in set/create actions."
        },
        "log_level": {
          "type": ["string", "null"],
          "default": null,
          "description": "Override log level for this action."
        }
      }
    },

    "ErrorHandlerSpec": {
      "type": "object",
      "description": "An error handling rule that matches errors from a specific source and applies actions.",
      "required": ["source"],
      "additionalProperties": false,
      "properties": {
        "source": {
          "type": "string",
          "description": "Snake_case primitive name this handler matches, or empty string for catch-all."
        },
        "source_type": {
          "type": "string",
          "enum": ["content", "channel", "compute", "boundary", ""],
          "default": "",
          "description": "Which primitive type the source is."
        },
        "boundary": {
          "type": ["string", "null"],
          "default": null,
          "description": "Snake_case Boundary name this handler is scoped to."
        },
        "condition_expr": {
          "type": ["string", "null"],
          "default": null,
          "description": "CEL expression evaluated against the error context. Handler fires only when truthy."
        },
        "actions": {
          "type": "array",
          "items": { "$ref": "#/$defs/ErrorActionSpec" },
          "default": [],
          "description": "Actions to take when this handler matches."
        },
        "is_catch_all": {
          "type": "boolean",
          "default": false,
          "description": "Whether this is a catch-all handler (matches all errors in its scope)."
        }
      }
    },

    "FieldDependency": {
      "type": "object",
      "description": "A field-level data dependency for a Compute module. Declares that the Compute reads a specific field from a Content, and the confidentiality scopes required to access it.",
      "required": ["content_name", "field_name"],
      "additionalProperties": false,
      "properties": {
        "content_name": {
          "type": "string",
          "description": "Snake_case Content name containing the field."
        },
        "field_name": {
          "type": "string",
          "description": "Snake_case field name within the Content."
        },
        "confidentiality_scopes": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "Confidentiality scopes required to read this field. Inherited from the field's own confidentiality_scopes if not overridden here."
        }
      }
    },

    "ReclassificationPoint": {
      "type": "object",
      "description": "A declared point where data changes confidentiality level. Reclassification only happens through Compute modules — this makes confidentiality transitions explicit, auditable, and compiler-verified.",
      "required": ["compute_name", "input_scopes", "output_scope"],
      "additionalProperties": false,
      "properties": {
        "compute_name": {
          "type": "string",
          "description": "Snake_case name of the Compute module performing the reclassification."
        },
        "input_scopes": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Confidentiality scopes of the input data consumed by this Compute."
        },
        "output_scope": {
          "type": "string",
          "description": "Confidentiality scope assigned to the output data produced by this Compute."
        }
      }
    }
  }
}
